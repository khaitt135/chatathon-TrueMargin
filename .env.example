# True Margin
# Not read by index.html. The dashboard, rule engine, and demo data all
# work with no keys. A live Claude draft is attempted from the browser
# and labelled on screen; it falls back to a local template if the call
# fails (the usual case without a proxy).

ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
