# True Margin
# Copy to .env only if you're wiring up a live delegation-message draft
# (Decision 2 = live API). The dashboard, rule engine, and demo data all
# work with no keys set.

ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
